export interface CryptoData {
  bitcoin: { usd: number };
  ethereum: { usd: number };
  dogecoin: { usd: number };
}

// this interfaxe ensure typescript will expect each cryptocurreny to have a USD price.


